services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: eventflow
      POSTGRES_PASSWORD: eventflow
      POSTGRES_DB: eventflow
    ports:
      - "5432:5432"
    volumes:
      - eventflow-db-data:/var/lib/postgresql/data

volumes:
  eventflow-db-data:
